<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.attentipteamcharmander">

    <application
        android:allowBackup="true"
        android:icon="@drawable/bannerapp"
        android:label="@string/app_name"
        android:roundIcon="@drawable/bannerapp"
        android:supportsRtl="true"
        android:theme="@style/Theme.AttentipTeamCharmander">
        <activity android:name=".ProfileActivity"></activity>
        <activity android:name=".HistorialEstadoA" />
        <activity android:name=".OnBoarding" />
        <activity android:name=".MainActivity" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>